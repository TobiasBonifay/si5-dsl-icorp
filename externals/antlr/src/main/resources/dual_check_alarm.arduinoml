application dualCheckAlarm

# declaring bricks
sensor button: 9
sensor button2: 12
actuator buzzer: 13

# Declaring states
on {
    led <= HIGH
    buzzer is LOW => on
}

-> off {
    led <= LOW
    buzzer is HIGH => off
}

initial "off"

# from "on" to "off" when "button" becomes "low" and "button2" becomes "low"
#transition "on" -> "off" when button becomes LOW and button2 becomes LOW